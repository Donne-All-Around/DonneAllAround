server:
  port:
    5000

spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/donnearound?useUnicode=true
    username: ssafy
    password: ssafy

fcm:
  service-account-file: firebase/donnearound-firebase-adminsdk-access-key.json #비밀 키 경로
